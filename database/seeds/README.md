# seeds

Development-only SQL seeds.

## What lives here

- Sample users, a demo server, channels (passwords hashed consistently with Identity)

## Remaining tasks

- [ ] Optional `dev_seed.sql` for local demos
- [ ] Document how to run seeds (never auto-run in production)
