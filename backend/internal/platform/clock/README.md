# platform/clock

Clock abstraction for deterministic tests.

## What lives here

- `Clock` interface (`Now()`)
- Real and fake implementations

## Remaining tasks

- [ ] Inject into session expiry, invite expiry, presence idle calculation
