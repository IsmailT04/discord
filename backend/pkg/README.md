# pkg

Public-ish libraries safe to import outside `internal`.

## What lives here

- Keep **almost empty** on purpose
- Only put code that could theoretically be extracted as a module

## Remaining tasks

- [ ] Resist moving domain here
- [ ] If needed later: small helpers (e.g. set, optional) with no infra deps
