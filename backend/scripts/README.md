# scripts

Operational and developer scripts (bash/make helpers).

## What lives here

- Dev bootstrap helpers
- One-off ops scripts (backup reminder, key generation docs)

## Remaining tasks

- [ ] `dev-up` / `dev-down` wrappers around compose (when compose exists)
- [ ] Script to generate cookie/CSRF secrets for local env
- [ ] No business logic in scripts
