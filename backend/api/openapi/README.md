# api/openapi

OpenAPI (Swagger) contracts for the HTTP API.

## What lives here

- Versioned OpenAPI YAML/JSON
- Optional generated server stubs / client types (later)

## Remaining tasks

- [ ] `openapi.yaml` for Identity + health first
- [ ] Expand per shippable update
- [ ] CI drift check vs implemented routes (optional)
