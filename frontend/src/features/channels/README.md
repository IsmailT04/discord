# features/channels

Channel list within a server (text + voice entries).

## What lives here

| Path | Contents |
|------|----------|
| `api/` | Channel list/create/update APIs. |
| `components/` | Channel sidebar, category grouping (later). |
| `hooks/` | Channel queries and selection hooks. |

## Remaining tasks

- [ ] Render text/voice channels
- [ ] Create channel (permission-gated)
- [ ] Highlight active channel
- [ ] Unread badges (with notifications/chat later)
