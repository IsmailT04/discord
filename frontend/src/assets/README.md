# assets

Static frontend assets (images, fonts, icons) imported by the app.

## Remaining tasks

- [ ] Add brand/logo assets when design direction is chosen
- [ ] Prefer SVG icons; avoid huge binaries in git
